<?php
function func_check(array $param){
    $str = '';
    if(is_null($param) || is_bool($param)){
		$str .= '';
	}elseif(is_string($param) || is_numeric($param)){
		$str .= preg_replace('/[^0-9a-z]/i', '', $arg, -1);
	}else{
		ksort( $arg, SORT_STRING);
		foreach( $arg as $key => $value){
			$str .= ($key . '=' . $this->joins( $value, $mtkey, $api));
		}
	}
}
